[project]
name = "mealtracker-agentic-ai"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.110",
    "uvicorn[standard]>=0.29",
    "groq>=0.11.0",
    "python-dotenv>=1.0",
    "pydantic>=2.6",
    "python-multipart>=0.0.9",
    "httpx>=0.27",
]

[tool.vercel]
entrypoint = "api_server:app"